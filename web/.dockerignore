# node_modules will be reinstalled inside the container via `npm ci`.
# Keeping it out of the build context speeds the COPY significantly.
node_modules/

# Build output — regenerated by `npm run build` in the image
dist/
.vite/

# Editor / OS / CI
.vscode/
.DS_Store
Thumbs.db

# Tests / docs we don't ship
*.md
